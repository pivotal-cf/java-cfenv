buildscript {
    ext {
        jmockitVersion = "1.49"
        springBootVersion = "3.4.0"
    }
}

plugins {
    id 'base'
    id "io.spring.nohttp"
}

description = 'Java Library for Accessing Cloud Foundry Environment Variables'

wrapper {
    gradleVersion = "8.7"
}

