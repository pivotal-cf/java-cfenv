buildscript {
    ext {
        jmockitVersion = "1.49"
        springBootVersion = "3.0.5"
    }
}

plugins {
    id 'base'
    id "io.spring.nohttp"
}

description = 'Java Library for Accessing Cloud Foundry Environment Variables'

wrapper {
    gradleVersion = "7.1.1"
}
